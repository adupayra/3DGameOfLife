#version 330 core
out vec4 FragColor;

in vec3 ourColor;
in vec2 texCoord;

uniform sampler2D shaderTexture;
uniform sampler2D shaderTexture2;
uniform float step;

void main()
{
   FragColor = mix(texture(shaderTexture, texCoord),texture(shaderTexture2, texCoord), step);
}